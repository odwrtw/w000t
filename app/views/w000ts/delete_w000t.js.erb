if ($(".w000ts.my_image_index").length) {
  $("figure#w000t-<%= escape_javascript w000t.short_url %>").remove();
  $("div#w000t-wall-container").masonry();
} else if ($(".w000ts.my_index").length) {
  $("tr#w000t-<%= escape_javascript w000t.short_url %>").remove();
}
