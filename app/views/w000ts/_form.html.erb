<%= form_for(w000t) do |f| %>
  <% if w000t.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(w000t.errors.count, "error") %> prohibited this w000t from being saved:</h2>

      <ul>
      <% w000t.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :long_url, class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit 'w000t it', class: "btn btn-primary btn-lg btn-block" %>
  </div>
<% end %>
