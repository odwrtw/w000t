// We fillUp the #w000t element and show it
$("#w000t").html("<%= escape_javascript w000t.full_shortened_url(request.base_url) %>").removeClass('hide');

// Then we select its text
$("#w000t").selectText();
